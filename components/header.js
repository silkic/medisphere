import React from 'react';
import logo from '../img/logo.png';

const Header = () => {
  return (
    <header>
      <img src={logo} alt="MediSphere Logo" height="40" />
    </header>
  );
};

export default Header;